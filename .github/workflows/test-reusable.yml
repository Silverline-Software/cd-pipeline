name: Test Reusable v5a (permissions only)

on:
  workflow_call:
    inputs:
      project-display-name:
        type: string
        default: ''

permissions:
  contents: read
  checks: read

jobs:
  hello:
    name: Hello World
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "permissions ok"
