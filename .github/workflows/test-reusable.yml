name: Test Reusable v5c (no permissions at all)

on:
  workflow_call:
    inputs:
      project-display-name:
        type: string
        default: ''

jobs:
  hello:
    name: Hello World
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "no permissions"
