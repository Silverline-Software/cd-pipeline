name: Test Reusable - No Permissions

on:
  workflow_call:
    inputs:
      project-display-name:
        type: string
        default: ''

jobs:
  hello:
    name: Hello World
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "no permissions test"
